
// Fix: Added React import to provide the React namespace for type definitions like React.ReactNode
import React from 'react';
import './globals.css';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata = {
  title: 'Telangana Labour Department - Integrated Dashboard',
  description: 'Role-based integrated digital dashboard for the Labour Department, Telangana.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-slate-50 text-slate-900`}>
        {children}
      </body>
    </html>
  );
}
